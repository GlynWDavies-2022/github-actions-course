name: 02 - Workflow Events

on:
    push:
    #pull_request:
    #schedule:
    #    - cron: '22 20 * * *'
    # workflow_dispatch:

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Show the trigger
              run: echo "I was triggered by the ${{ github.event_name }} event!"
